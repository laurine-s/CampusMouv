{% extends 'base.html.twig' %}

{% block title %}Sorties disponibles{% endblock %}

{% block main %}
    <section class="uk-section uk-section-default uk-flex uk-flex-center">
        <div class="uk-container">
            <h1>Les sorties disponibles</h1>
        </div>
    </section>

    <section class="uk-section uk-section-default uk-flex uk-flex-center">
        <div class="uk-container">
            <div class="uk-grid uk-grid-small">
                <div class="uk-width-1-3@m">
                    {#                    {% for sortie in allSorties%} #}
                    <a class="uk-text-decoration-none" href="
{# {{ path('sortie_detail', {id: sortie.id}) }} #}
">
                        <div class="uk-card uk-card-default uk-border-rounded">
                            {#                        <div class="uk-card-header"><img src="{{ sortie.photo }}"></div> #}
                            <div class="uk-card-header">
                                {#                            <img src="{{ sortie.photo }}"> #}
                                Image
                            </div>
                            <div class="uk-card-body">
                                <div class="">nom
                                    {#                                {{ sortie.nom }} #}
                                </div>
                                <div class=""> infos
                                    {#                                {{ sortie.infos }} #}
                                </div>

                            </div>
                            <div class="uk-card-footer">
                                <div class="uk-flex uk-flex-left uk-flex-middle uk-flex-wrap uk-grid-small" uk-grid>
                                    <div class="uk-border-pill uk-border-rounded">
                                        <p>Début :
                                            {# {{ sortie.date_heure_debut }} #}
                                        </p>
                                        <p>Fin d'inscription :
                                            {# {{ sortie.date_heure_fin }} #}
                                        </p>
                                    </div>

                                    <div class="uk-border-pill uk-border-rounded">
                                        <p>Participants :</p>
                                        {#                                                {{ sortie.nb_inscrits }} #}
                                        <p> 1
                                            sur
                                            {# {{ sortie.nb_inscription-max }} #}
                                            5</p>
                                    </div>

                                    <div class="uk-border-pill uk-border-rounded">
                                        <p>Campus de : Rennes</p>
                                        <p>Organisé par : TOTO</p>
                                    </div>
                                </div>
                            </div>


                        </div>
                    </a>
                    {#                    {% endfor %} #}
                </div>
            </div>
        </div>
    </section>

{% endblock %}
