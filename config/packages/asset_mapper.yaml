framework:
  asset_mapper:
    paths:
      - assets/
    missing_import_mode: ignore  # Plus permissif pour éviter les erreurs

when@prod:
  framework:
    asset_mapper:
      missing_import_mode: ignore
      public_prefix: '/assets'